// import SysConfig from "types/sysConfig"

export const sysConfig = {
  port: 2099,
  pwdSalt: "awsl",
  tokenSalt: "awsl"
}

export const databaseConfig = {
  host: "localhost",
  name: "qiniu",
  user: "root",
  password: "root"
}
